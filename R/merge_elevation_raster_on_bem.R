merge_elevation_raster_on_bem <- function(rstr, bem) {
  # compute mean of slope of raster's cells in each polygon

  # compute circular mean of aspect  raster's cells in each polygon

  # compute mean of elevation raster's cells in each polygon

  # create indicator base on elevation treshold

}
